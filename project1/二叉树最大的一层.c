#include <stdio.h>
#include <stdlib.h>
typedef struct bintree{
	int data;
	struct bintree *left,*right;
}node;
typedef struct queue{
	node *ele[maxsize];
	int front;
	int rear;
}queue; 
int main(){
	node *root=NULL;
	int n;
	scanf("%d")
	return 0;
}
